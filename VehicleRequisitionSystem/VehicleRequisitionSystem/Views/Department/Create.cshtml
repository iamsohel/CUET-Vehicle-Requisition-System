@model VehicleRequisitionSystem.Models.Department

@{
    ViewBag.Title = "Add Department";
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

        <!-- Content Wrapper. Contains page content -->
    <div class="content-wrapper">
        <!-- Content Header (Page header) -->
        <section class="content-header">
            <h1>
                Add Department Form
            </h1>
            <ol class="breadcrumb">
                <li><a href="#"><i class="fa fa-dashboard"></i> Home</a></li>
                <li class="active">Add Department </li>
            </ol>
        </section>
        <!-- Main content -->
        <section class="content">

            <!-- Main row -->
            <div class="row">
                <!-- Left col -->
                <div class="col-md-7">
                    <!-- Horizontal Form -->
                    <div class="box box-primary">
                        <div class="box-header with-border">
                            <h3 class="box-title">Add Department</h3>
                        </div>
                        <!-- /.box-header -->
                        <!-- form start -->

                        <div class="box-body">
                            <div class="form-group">
                                <label for="exampleInputEmail1">Department Name</label>
                                @Html.TextBoxFor(model => model.DepartmentName, new { @class = "form-control", @id = "exampleInputEmail1", @placeholder = "Department Name" })
                                @Html.ValidationMessageFor(model => model.DepartmentName)
                            </div>
                            <div class="form-group">
                                <label for="exampleInputEmail1">Short Name</label>
                                @Html.TextBoxFor(model => model.ShortName, new { @class = "form-control", @id = "exampleInputEmail1", @placeholder = "Short Name" })
                                @Html.ValidationMessageFor(model => model.ShortName)
                            </div>

                        </div>
                    </div>
                    <!-- /.box-body -->
                    <div class="box-footer">
                        @Html.ActionLink("Cancel", "Index", "Department", null, new { @type = "submit", @class = "btn btn-default" })

                        <button type="submit" class="btn btn-primary">Submit</button>
                    </div>

                </div>
                <!-- /.box -->
                <!-- general form elements disabled -->
                <!-- /.box -->
            </div>
            <!-- /.Left col -->
            <!-- right col (We are only adding the ID to make the widgets sortable)-->
            <!-- right col -->
        </section>
    </div>
            <!-- /.row (main row) -->

        <!-- /.content -->



}


